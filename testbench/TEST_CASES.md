# vip_chi testbench testcase catalog

The shared regression currently runs **234 SystemVerilog** testcases (one
`` `include `` per `tc_*.sv` in `sv/tc/chi_tc_pkg.sv`) and **235 pyUVM/cocotb**
testcases (`tc_*.py` discovered by `py/scripts/run.py`). Those counts are
maintained here as part of adding a testcase, not re-derived: adding one means
adding its row below and updating this paragraph.

One testcase is **SV only**: `tc_chi_e_hni_port1`, because the SV CHI-E proxy is
2×2 and this port's is 1×1, so RN port 1 and SN target 1 have no interfaces to
drive. That asymmetry is recorded where the binds are declared in both
`chi_e_proxy_tb_env` files.

The lists are otherwise identical apart from two Python-only testcases,
`tc_chi_sva_smoke` and `tc_chi_reject_scope`. The second tests a mechanism
the SV port does not need: SV catches a driver's refusal with a
`uvm_report_catcher`, which seven existing negative controls exercise every
sweep, while this port had no way to observe a refusal raised inside a
coroutine the agent started. `py/vip_chi_reject.py` closes that and
`tc_chi_reject_scope` is what says it works. The first is described below.

On `tc_chi_sva_smoke`,
which exists only on the Python side. It is the negative control for
`py/sva/bind_chi.py`, and the SV checker it mirrors reports through SVA
`assert property ... else $error`, which a `uvm_report_catcher` cannot demote
(the same wall `tc_chi_dataid_duplicate` hit, which is why that test stands the
check down through `dat_reorder_allowed` instead of catching it fire). Writing
the SV twin therefore needs assertion-control plumbing that does not exist yet,
or the SV checks moved onto `uvm_error`. Until then this row is Python-only ON
PURPOSE. Note for `.refuse.yml`: profiles may not name it explicitly, because a
selector matching nothing is an error on the SV side; the `full` profile
matches it via `^tc_.*$`, which expands per flow.

Each testcase exercises a topology purely by which agents it drives (see [sv/UVM_TB.md](sv/UVM_TB.md) §2 for the SV harness model)
and checks observed monitor items against sequence responses or expected
payloads. This catalog is the authoritative list.

Most coherent scenarios run at both CHI-D and wide CHI-E from a single
parameterized body: `vip_chi_<scenario>_base_test #(CFG_P, TYPES_T)` extends the
parameterized `chi_coherent_base_test`, and each runnable name lives in its
own file (one class per file): `tc_chi_coh_d_<x>.sv` fixes CHI_D_CFG_C /
chi_d_types_t and `tc_chi_coh_e_<x>.sv` fixes CHI_E_WIDE_CFG_C /
chi_e_wide_types_t. Both names run independently; there is no separate
hand-maintained CHI-E scenario copy.

## Mode Legend

| Mode | Meaning |
| --- | --- |
| `n/a` | Object/unit smoke that does not build a link-level testbench. |
| `RNI` | RN-I sequence/unit smoke without a full integrated datapath. |
| `INT` | Integrated RN-I↔SN-F datapath. |
| `E-RNI` | Exact CHI-E RN-I-side smoke. |
| `E-SNF` | Exact CHI-E SN-F-side manual completion smoke. |
| `E-SNF-A` | Exact CHI-E autonomous SN-F responder smoke. |
| `E` | Wide CHI-E RN-I↔SN-F integrated datapath. |
| `HNI` | HN-I proxy path through one RN and one SN port. |
| `HNI-FANIN` | HN-I proxy path with multiple RN ports targeting one SN. |
| `HNI-XBAR` | HN-I proxy crossbar path with address-routed SN ports. |
| `COH` | Coherent RN-F/HN-F subsystem over SNP-capable links. |

### pyUVM/cocotb port

The Python port (`py/`) uses one Verilator HDL shell and one Python
testbench top:

```text
py/tb/chi_hdl_top.sv
py/tb/chi_tb_top.py
py/vip_chi_agent_example_py.core
```

`chi_hdl_top.sv` exposes all flat-net endpoint groups with unique prefixes.
`chi_tb_top.py` creates the matching `ChiBus` objects, publishes them
through pyUVM `ConfigDB`, and contains one static cocotb test entry per public
`tc_*.py` testcase. The cocotb test name, command-line name, testcase file, and
pyUVM test class all use the same public `tc_*` name.

Run it from `testbench/py`:

```sh
./scripts/run.py --build
./scripts/run.py -t tc_chi_d_read_smoke --no-build
./scripts/run.py --all --no-build
```

The script uses FuseSoC for the Verilator build/run commands. FuseSoC does not
provide a project-aware `--all` switch, so the script discovers the static
cocotb tests in `chi_tb_top.py` by their public `tc_*` names and runs one
simulator process per testcase.

Both flows run the same list of testcase names, with the single documented
exception of `tc_chi_sva_smoke` described at the top of this file.

---

## Building-block smokes

| Test | Mode | Proves |
| --- | --- | --- |
| `tc_chi_cfg_item_smoke` | n/a | cfg-item defaults and `reset()` behavior. |
| `tc_chi_cfg_invalid` | n/a | `vip_chi_cfg_agent::is_valid()` runtime self-check: one case per rule, each starting from a default config and breaking exactly one thing, plus legal and warn-only configs that must stay accepted. Catches both a rule that stops working and a rule that starts rejecting a legal setup. |
| `tc_chi_item_smoke` | n/a | item randomization, legality, copy/compare, and payload handling across CHI-D and CHI-E shapes. |
| `tc_chi_base_seq_smoke` | RNI | base-sequence helpers, wrapper sequences, and the write-zero legality path. |
| `tc_chi_opcode_pool_safe` | n/a | `PCrdReturn` is not drawable from the non-coherent legal-opcode pools: an RN-I item randomized many times across both directions and both issues never lands on it, and every drawn opcode is accepted by the legality helper. A drawn `PCrdReturn` would wedge the driver with no diagnostic. The coherent RN-F pool is drawn the same way and cross-checked against the helper, so the two hand-written opcode tables cannot drift apart. |
| `tc_chi_reject_scope` | n/a | the Python port's negative-control mechanism, tested as a mechanism (`py/vip_chi_reject.py`). A driver's refusal is an exception inside a coroutine the agent started, so nothing could observe it as a tested outcome and every control of that shape existed in SV only. Checks that an unarmed refusal still raises, that an armed one records and RETURNS so the driver carries on as a demoted `uvm_fatal` does, that a scope which provokes NOTHING fails, that a scope for another rule does not swallow it, and that a wrong count fails in either direction. Python-only; see the note at the top of this file. |
| `tc_chi_sva_smoke` | n/a | negative control for the Python link-layer protocol checker (`py/sva/bind_chi.py`): one induced violation per check family — flitpend without flitv, a flit sent during one-sided ACTIVATING, L-credit underflow and overflow, TXSACTIVE held past deactivation, channel traffic during reset, and a link that never reactivates — each asserted to be REPORTED. Plus two positive controls that must NOT fire: a same-cycle credit grant and consume (the race that false-fired two earlier SV attempts) and a link that reactivates inside the window. Every other testcase passes with the checkers enabled, so without this a vacuous checker would pass exactly as loudly as a working one. Python-only; see the note at the top of this file. |
| `tc_chi_a0_smoke` | n/a | the interface and `chi_link_adapter` at a third flit geometry (7-bit node IDs, 32-byte data): an agent-free link-activation handshake and one REQ flit checked verbatim on the far side. The Python twin additionally checks its packing codec against the HDL net widths — a check with no SV counterpart, since the SV interface carries the flit struct itself. |

## Integrated non-coherent datapath

| Test | Mode | Proves |
| --- | --- | --- |
| `tc_chi_d_read_smoke` | INT | `ReadNoSnp` request and `CompData` return. |
| `tc_chi_d_write_read_smoke` | INT | write to the SN-F backing store then read the same payload back. |
| `tc_chi_d_write_partial_smoke` | INT | byte-enable-masked writeback and readback. |
| `tc_chi_d_ordered_write` | INT | `CompDBIDResp` followed by `CompAck` for ordered writes. |
| `tc_chi_d_ordered_read` | INT | ordered `ReadNoSnp` returns `ReadReceipt` before `CompData`. |
| `tc_chi_txsactive_window` | INT | `TXSACTIVE` is held across the whole outstanding window rather than pulsed per flit. Four pipelined reads, both link ends sampled every cycle: each vantage must show at least one cycle asserted with no flit moving (which a per-flit pulse cannot produce), must never drop between its first assertion and the last flit, and must drop once the traffic drains. |
| `tc_chi_item_timestamps` | INT | per-transaction timestamps on the observed item. Asserts the milestones are populated, are monotonic (a request cannot be granted before it was issued, nor a burst end before it began), that `data_burst_time()` agrees with the beats it derives from, and that `latency()` equals what the perf counters measured for the same read — two paths to one interval, so disagreement localizes the bug to one of them. |
| `tc_chi_latency_bound` | INT | per-transaction latency bounds, both halves. A generous bound (10000 cycles) must stay silent on ordinary traffic; a bound of 1 cycle must flag the same read exactly once, with the monitor's counter and the report agreeing. The bound is tightened rather than the completer slowed, so the test does not assert on a margin that depends on how delays happened to land. |
| `tc_chi_ordered_stream` | INT | ordered-stream acknowledgement order, positive case. Six pipelined ordered writes then six pipelined ordered reads, both deep enough that the completer holds several at once. The scoreboard must have compared every acknowledgement (12) and found none out of place; the in-order tally is asserted too, so a run where the check never compared anything cannot pass as clean. |
| `tc_chi_ordered_stream_negctl` | INT | negative control for the same check. `snf_reorder_ordered_service` has the buffered SN-F serve one pair of queued ordered requests back to front; every read still completes correctly, so no other checker can see the fault. The inversion must be flagged exactly once (not cascaded) and the rest of the stream still compared in order. |
| `tc_chi_ordered_write_no_comp_ack` | INT | ordered writes WITHOUT `ExpCompAck`, a combination no other test covered. `Order` and `ExpCompAck` are independent -- the first asks the completer to acknowledge in receipt order, the second asks for a separate requester-driven acknowledgement -- but every other ordered-write test set both, so the pipeline only ever retired ordered writes on the CompAck path. Six pipelined ordered writes must each hand back a combined `CompDBIDResp` and be acknowledged in order; the completion opcode is asserted, not just the response count, so a run that merely failed to deadlock does not pass as clean. |
| `tc_chi_lasm_illegal_transition` | INT | negative control for the link-activation state machine. `lasm_abort_activation` has the requester raise `txlinkactivereq` and withdraw it before the completer acknowledges, so the link leaves `ACTIVATE` without reaching `RUN`. Both binds must count the aborted bring-up, the legal activation that follows must not be flagged, and a read must still complete over the recovered link. `tb_cfg.lasm_illegal_expected` suppresses the `$error` while leaving the count intact -- the assertion-control plumbing this file's header notes was missing, now present for this one rule. |
| `tc_chi_lasm_input_race_negctl` | INT | negative control for the OBSERVER half of IHI 0050 E §14.6.3 / D §13.6.3 -- *"a component that observes the input race is required to wait for both signals before changing any output signals"*. Takes TWO knobs, which is the shape of the requirement: `lasm_abort_activation` on the requester produces a pair of outputs arriving at the completer out of order, and `lasm_ignore_input_race` on the completer then moves its outputs through it. Exactly one report at the completer and none at the requester, and the link must still carry a read. It exists because fixing the VIP removed the rule's only failing observation, leaving it unable to fail anywhere. |
| `tc_chi_lasm_output_race_negctl` | INT | negative control for IHI 0050 E §14.6.3 / D §13.6.3's **second** ordering -- *"the deassertion of RXACK must not occur before the deassertion of TXREQ"*. All four orderings report under one check id, and this was the only one nothing provoked: the aborted activation reaches the fourth and then the first, the tear-down race reaches the third, so the rule read as exercised while a quarter of it had never failed. `lasm_ack_falls_first` drops the completer's acknowledge once while its own request is still up. One banned step, three reports: the ordering at the completer alone (which names who is at fault) and the illegal `RUN -> ACTIVATE` step at both ends. |
| `tc_chi_coh_d_snp_flitpend` | COH | the SNP twin of `tc_chi_flitpend_without_valid`, and the last rule in the registry no test could reach. `CHI_SNP_VALID_REQUIRES_PEND` had never once been evaluated, because nothing raises SNP FLITPEND -- the home pairs it with its snoop. `cfg.flitpend_without_valid` on the HN-F pulses a lone one per RN link, and the rule must report exactly once per bind that saw it; snooped coherent traffic then follows so anything firing on ordinary snoops would push the count past one. Note the home drives SNP FLITPEND LOW on real snoops, so this control is the rule's only evaluation anywhere -- exercised but THIN, which is the honest description. |
| `tc_chi_e_tag_integrity` | E | MTE tag read-back integrity. The exact-CHI-E completer has kept a per-beat tag store and replayed it since it was written, and nothing ever checked what came back -- a model nothing checks is the same defect as a check nothing exercises, seen from the other side. A tagged write then a read; the scoreboard predicts tag, TagUpdate and TagOp alongside the data. Asserts that tags were **compared** and none mismatched: zero out of zero is not a pass, and a scoreboard that predicted nothing would satisfy "no mismatches" too. |
| `tc_chi_e_tag_negctl` | E | negative control for the tag checks. `cfg.snf_corrupt_tag` flips the returned tag AND the returned TagOp -- two independent breakages, because a corrupt tag store and a completer that invents a TagOp fail differently, and a control breaking only one would leave the other unproven. Both land on the same beat because the only MTE-capable link is 64 B and CHI's maximum transfer is 64 B, so every MTE transfer here has exactly one beat. |
| `tc_chi_e_tag_match` | E | The Tag Match operation, in four phases against one address. Table 13-34 puts the obligation on the completer under `TagOp = Match`: "the Physical Tags in the write must be checked against the Allocation Tag values obtained from memory". An Update write establishes the Allocation Tag and is owed no response; a Match carrying the same tag is answered **Pass**; a Match carrying a tag one bit away is answered **Fail**, so the result is a function of the tags and not a constant, which one phase cannot show; and a Match carrying the original tag again is answered **Pass**, which is what proves the failing Match in between did not WRITE its tag -- Update stores, Match checks, and a completer that committed on Match would make the last phase report Fail. `CHI_SB_TAG_MATCH_OWED` and `CHI_SB_TAG_MATCH_RESULT` must each record three passes and no failure, and a TagMatch must be observed on the wire every time, since both rules are silent against a completer that answered nothing. |
| `tc_chi_e_tag_match_negctl` | E | Negative control for `CHI_SB_TAG_MATCH_OWED`. `cfg.snf_tag_match_unrequested_negctl` has the completer answer whether or not the data asked, and the write carries `TagOp = Transfer` -- so the TagMatch that comes back is owed to nobody. Not harmless: a Requester tracking Match completions by counting them goes permanently out of step while every field on the wire stays legal. `ReturnNID` is pinned because a write that asks for no Match gets no requester-defaulted return route, and without it the unrequested response would provoke an orphan rather than the rule under test. |
| `tc_chi_e_tag_match_result_negctl` | E | Negative control for `CHI_SB_TAG_MATCH_RESULT`, which no stimulus alone can make fail: a completer that compares correctly agrees with the scoreboard's tag shadow on every write, because both read the same tags. `cfg.snf_tag_match_invert_result_negctl` has the completer report the opposite of what it found -- the shape of the original defect, where the responder filled the field from the cache-state enum and Resp.I's three bits are also Table 13-25's Fail, so every Match was answered Fail while a response still arrived each time. An Update write first, so the following Match has a real answer to invert; the response must carry Fail where the tags matched; `CHI_SB_TAG_MATCH_RESULT` must report exactly once and `CHI_SB_TAG_MATCH_OWED` not at all, which is what shows the two rules are separable rather than one rule counted twice. |
| `tc_chi_recording_smoke` | INT | transaction recording, checked for the two things that can be wrong with it rather than for "it did not crash". A recorder that opened every stream and closed none would pass a naive smoke test, so this asserts the LIFECYCLE: every stream opened must have been closed (the leak check, which a passing run cannot otherwise show), and opens must equal closes (which catches a stream closed on the wrong object -- `end_tr` must be called on the item `begin_tr` opened, and the completion arrives as a different item on a different channel). A read and a write, because they complete on different channels and each exercises a different close path. |
| `tc_chi_flitpend_without_valid` | INT | negative control for the two FLITPEND rules, which no other test could reach. `CHI_REQ_VALID_REQUIRES_PEND` and `CHI_RSP_VALID_REQUIRES_PEND` had never once been evaluated anywhere in either regression, because nothing raised FLITPEND on those channels -- the drivers pair it with its flit, and only the DAT burst has a use for it. `cfg.flitpend_without_valid` pulses a lone FLITPEND on REQ and RSP once after link-up; each rule must report EXACTLY once (fewer means still vacuous, more means it is also firing on the well-formed FLITPEND of the write that follows). Note the rule holds a VIP EMISSION CONVENTION, not a CHI mandate: CHI's FLITPEND is a one-cycle-ahead hint and a lone one is legal there. |
| `tc_chi_lasm_race` | INT | the LASM under a RACE rather than a malformed sequence, and a test that could not exist before graceful deactivation did -- there was no tear-down to race with. `cfg.lasm_reactivate_during_deactivate` has the requester change its mind half way through: the link is in `DEACTIVATE` with the completer still returning credits, and the request goes up again, making the pair `{1,1}` = `RUN` -- a jump the cycle forbids. What makes it a race rather than a wrong sequence is WHEN it lands, inside a window no uniform delay could reach. Both binds must report it, and the link must still recover and carry traffic. |
| `tc_chi_lasm_deactivate` | INT | the tear-down half of the link activation state machine, which reset alone can never reach. Traffic first, so the link is genuinely `RUN` with credits banked at both ends; then `cfg.link_deactivate_request` takes it down and it must reach `STOP` with EVERY L-credit returned and none advertised on the way; then it comes back up and carries traffic again. The last phase is what makes the middle one non-trivial -- reaching `STOP` by wedging the link would be easy, and only traffic afterwards tells a clean deactivation from a broken one. Also asserts that `CHI_LINK_DEACTIVATE_WHEN_IDLE` was EVALUATED, since it could not reach its own antecedent before this path existed. |
| `tc_chi_lasm_timeout` | INT | negative control for the two link timeouts. `lasm_stall_activation_cycles` withholds the completer's acknowledge and `lasm_stall_deactivation_cycles` withholds its drop, each for four times the configured bound, so the link sits in `ACTIVATE` and then in `DEACTIVATE` past the limit. Both binds must report each episode ONCE -- on the crossing, not per cycle, which would bury the one useful line under thousands of copies -- and the link must recover from each, since a reported timeout is a diagnostic rather than a wedge. The stalls are deliberately finite for that reason. |
| `tc_chi_check_disable` | INT | negative control for the per-check enable. One rule is disabled by ID before any traffic; on the same write burst it must record NOTHING -- not a pass, not a fail -- while a sibling rule on the transmit side still records passes. The sibling is what proves the disable was targeted rather than taking the whole bind down, which would otherwise be invisible because both rules would read zero. A disabled rule is also excluded from the vacuity report, so "switched off" stays distinguishable from "never ran". |
| `tc_chi_check_vacuity` | INT | the vacuity report checked against itself. `CHI_COMPACK_WITHOUT_EXPCOMPACK` cannot be evaluated by read-only traffic, so after a read it must read as unexercised while a rule the read does exercise must not; a following write with `ExpCompAck` drives a CompAck and the same rule must move out of that state, having recorded a pass. Asserting the TRANSITION rather than a snapshot is what makes it non-tautological -- a hard-coded answer passes the first half and fails the second. |
| `tc_chi_sb_vacuity` | INT | the same argument as the row above, applied to the SCOREBOARD half of the registry -- the half that had no pass counts at all until it was named, so a rule that stopped evaluating and a rule that always held produced the identical log. An unordered read cannot reach `CHI_SB_ORDERED_ACK_IN_ORDER`, so it must read as unexercised while `CHI_SB_TXN_COMPLETES`, which that same read retires into, must not; an ordered read then moves it out. The pass COUNT is asserted too, not just the rule's absence from the list: a rule leaves that list on its first FAILURE just as readily, so absence alone would also be satisfied by a completer answering out of order. |
| `tc_chi_d_split_write_rsp` | INT | `DBIDResp` plus deferred `Comp`, with optional trailing `CompAck` under `ExpCompAck`. |
| `tc_chi_d_prefetch_tgt` | INT | `PrefetchTgt` treated as a no-completion hint. |
| `tc_chi_d_atomic` | INT | atomic store/load/swap/compare smoke using the SN-F backing memory for operand capture, RMW, old-data return, and readback. |
| `tc_chi_d_atomic_compare_size_legal` | D | The **positive** control for `CHI_ATOMIC_SIZE_LEGAL`. The five wide-operand atomic testcases prove the rule fires — they drive Sizes Table 2-17 does not list, arm it at `OFF` and require it to have reported — and a rule that fired on everything would pass all five. This drives the case a plausible implementation gets wrong instead: AtomicCompare's Size is the *combined* compare+swap size, so the table gives it a ceiling one step above the ordinary atomic limit, and deriving that ceiling from the 8-byte limit rejects a legal 32-byte request. The rule stays **armed**, and the test requires zero reports and at least one pass. Nothing is opted into, which is the point now: Table 2-17's Sizes are what a plain sequence draws, so the solver's view of the table is exercised beside the checker's — a regressed ceiling fails randomization rather than quietly drawing something else. |
| `tc_chi_d_atomic_variants` | INT | broader atomic sweep: `AtomicStore[0:7]`, `AtomicLoad[0:7]`, `AtomicSwap`, matching/non-matching `AtomicCompare`. |
| `tc_chi_d_atomic_predict` | INT | scoreboard Checker-C atomic RMW prediction: each variant seeds its target with a known full-beat write, issues the atomic, then reads back — the scoreboard independently predicts the returned pre-op value and the committed post-op value, keeping the atomic predictor from silently skipping. |
| `tc_chi_e_write_zero_readback` | E | `WriteNoSnpZero` memory semantics: a non-zero line is zero-written with no DAT phase, then read back as all zeros. The completion is a *write* completion — `CompDBIDResp`, or `DBIDResp` then `Comp` under `cfg.split_write_rsp` — not the bare `Comp` it looks like it should be: the requester sends no data, so the granted buffer is never used, but the response shape is normative regardless. |
| `tc_chi_e_write_unique_zero_negctl` | E | Negative control for the two identifier rules on `WriteUniqueZero`, the opcode no classifier used to claim. Section 2.3 gives a requester one live transaction per TxnID and section 2.6 says an accepted request completes; both rules are gated on the modeled-completion classifier, so an opcode it does not name is walked past in silence by both. Phase A raw-injects a `WriteUniqueZero` and its `CompDBIDResp`, requiring `CHI_COMPLETION_FOLLOWS_REQ` to record a pass — a rule that cannot arm cannot time out either. Phase B raw-injects the same opcode twice on one live TxnID with `CHI_TXNID_REUSE_*` at `OFF`, requiring exactly one report at each end of the link, and completes the duplicate so a timeout cannot be mistaken for the reuse report. |
| `tc_chi_e_raw_txsactive_negctl` | E | Negative control for the raw-inject path's TXSACTIVE window. `cfg.raw_req_txsactive_flit_scoped_negctl` reverts the raw path to the window it used to have — scoped to the injected flit, with nothing holding the sideband up while the transaction that flit started is still outstanding — and §14.7.2 requires TXSACTIVE to cover every outstanding transaction. Raw-injects one `WriteUniqueZero` and its `CompDBIDResp` with `CHI_TXSACTIVE_COVERS_OUTSTANDING` at `OFF`, requiring it to report and requiring `CHI_TXSACTIVE_DEASSERT_BOUNDED` to stay silent: a window closed too early is under-assertion and must not move the over-assertion bound. `tc_chi_e_write_unique_zero_negctl` is the positive half — same opcode, no compensating hold on the requester, no report. |
| `tc_chi_e_retry_field_negctl` | E | Negative control for the two retry field rules, both of which pass on every flit this VIP drives — which means neither failing branch has ever run. Phase A raw-injects a `WriteUniqueZero` carrying `AllowRetry` asserted together with a non-zero `PCrdType`, which section 2.9.4 forbids and which no driver here produces because the retry machinery sets `PCrdType` only on the re-issue. Phase B raw-injects a `PCrdReturn` carrying an address, which Table A-3 marks `0a`. Phase C raw-injects a bare `RetryAck` naming a TxnID no request has used, which section 2.6.5 forbids — the RN-I driver already fatals on this, but only while it is waiting on a specific request and only at the requester, so nothing judged the SN-F end. Each rule is required to report exactly once at each vantage with the report at `OFF`, the other rule is required to stay silent so the two are separable, and the whole registry is swept to prove the injected flits are not wrong in more ways than the two under test. |
| `tc_chi_txnid_reuse_srcid_scope` | E | The scope half of the TxnID rules. Section 2.5 requires a TxnID to be unique **for a given Requester**, "identified by the SrcID", so the rule has two halves that pull opposite ways: two SOURCES holding one TxnID is legal and on a fan-in link unavoidable, while ONE source reusing its own live TxnID is the violation — and it stays a violation after another source has touched the same value. Three raw-injected `WriteUniqueZero` flits walk it: A claims TxnID 0x73, B claims the same TxnID (must pass, and must record a pass — silence would mean the rule never evaluated), then A reuses its own live TxnID (must report exactly once at each end, with the report at `OFF`). Step 3 is the whole point: the shadow used to be one slot per TxnID with a note of the last claimant, so B's legal claim took ownership and A's reuse then passed — a MISSED violation, invisible to any ordinary test because the run is green either way. Completions are injected one per source and aimed by TgtID, which is how the checker decides whose claim a response retires. Verified non-vacuous in both directions: dropping step 3 fails with "reported 0", and making the shadow's key source-blind fails at step 2. |
| `tc_chi_comp_dbid_negctl` | E | Negative control for §2.5's DBID-equality rule: "A Comp response message sent separate from a `DBIDResp` or `DBIDRespOrd` message for a Write transaction must include the same DBID field value." Phase A raw-injects a split response whose `Comp` carries the granted DBID — must pass, and must *record* a pass, since the rule arms only on a separate grant and silence would mean it never evaluated. Phase B injects one whose `Comp` carries a different DBID — exactly one report at each end, with the report at `OFF`. Phase C drives a **real Atomic** through the SN-F's own responder with `split_write_rsp` on, and requires the rule to neither fail *nor pass*: two lines after the rule the same section makes the equality "permitted, but is not required" for atomics, so the exemption means the rule does not evaluate at all. The pass count is the discriminator there and the failure count is not — the SN-F's atomic completion carries matching DBIDs, so a rule with the exemption deleted would record a pass rather than a failure, and an assertion on failures alone would prove nothing. Verified by deleting the exemption: phase C then fails on the pass count. |
| `tc_chi_e_write_cmo` | E | combined Write + CMO: one request carrying a write and a cache maintenance operation to the same address, applied in that order. All six forms in one run — `Full` and `Ptl`, each with `CleanSh`, `CleanInv` and `CleanShPerSep` — because the six differ only in two independent choices and testing one would leave the other five as opcodes nothing ever put on a wire. The write half completes exactly like an ordinary write, so a completer that ignored the CMO entirely would look clean from the requester's side: what is asserted is the separate `CompCMO` as an OBSERVED flit, arriving after the write's own completion, with `Persist` after it for the two persistent forms. RSP fifo order is RSP channel order, so that is a real observation rather than a restatement of what the driver enforces. The three `Ptl` forms additionally set `ExpCompAck`, which the spec permits on a Non-CopyBack combined write: the `CompAck` must be sent AFTER the write's completion response and the spec sets no upper bound, so what is asserted is that bound and the presence/absence of the `CompAck` itself — not where it lands relative to `CompCMO`, which is the requester's choice. The three `Full` forms leave `ExpCompAck` clear, so both populations run here. The payload is read back and compared byte-wise under the write's byte enables — the `Ptl` forms only own the enabled lanes — because a completer that answered every response correctly and dropped the data would satisfy everything above. |
| `tc_chi_d_decerr_smoke` | INT | DECERR / NDERR behavior for writes and reads in a configured address window. |
| `tc_chi_d_derr_smoke` | INT | DERR-marked read data returned from the backing store. |
| `tc_chi_d_raw_inject` | INT | raw RN-I REQ + raw SN-F DAT/RSP injection, incl. verbatim observation of an illegal opcode (negative testing). |
| `tc_chi_dataid_out_of_order` | INT | DAT beats are placed by `DataID`, not by arrival: `snf_reverse_dat_beats` makes the SN-F return a 4-beat read in DESCENDING `DataID` and the payload must still reassemble in address order, in the monitor's item and in the requester's own response. The SVA `DataID`-ordering checks (which hold this VIP's in-order emission convention, not a CHI rule) stand down via `tb_cfg.dat_reorder_allowed`. |
| `tc_chi_dat_interleave` | INT | the beats of two reads share the DAT channel: `dat_interleave_depth = 2` makes the SN-F drain two queued 4-beat reads together, one beat each in turn, and both payloads must still arrive whole and in address order. A DAT flit is self-identifying (`TxnID` names the transaction, `DataID` the position) and CHI nowhere requires a transfer's beats to be contiguous. Checked on BOTH reassembly paths — the requester's own collector and the monitor's — because a receiver that reads the `FLITPEND` deassert as "this transfer ended" does not fail loudly here, it staples one read's beats onto another's and reports a data mismatch. Anti-vacuity: the emitted `TxnID` sequence must actually alternate (`n_dat_stream_switches > 0`); with the depth forced to 1 the test fails on "no interleaving reached the wire". The burst-shape assertions stand down via `tb_cfg.dat_interleave_allowed`; the per-`TxnID` retirement and the outstanding/`TXSACTIVE` pair stay armed. |
| `tc_chi_channel_delay` | D | the per-channel transmit delay reaches the wire, measured in cycles over four identical 8-write bursts. The knobs `{req,rsp,dat}_valid_delay_{enabled,min,max}` were declared, validated, documented and read by no driver in either port, so this test exists to make the difference between "configured" and "working" observable. `min == max` fixes the draw, because a random window makes the expectation a range and a safe range would pass on a delay that fired once. Growth against the baseline is deliberately NOT asserted to be `N x D`: a delay can overlap a wait the driver would have made anyway (chiefly an L-credit return), which is correct behaviour. What is asserted exactly is the STEP between two delay widths — going from `D` to `2D` must cost exactly `N x D` more, which is what says every request paid rather than one of them. A warm-up burst is discarded first, because the first burst after link bring-up is a few cycles slower than every later one. The control that matters: with the window still set and the enable off, the burst must take EXACTLY the baseline again. |
| `tc_chi_delay_gauss` | D | truncated-gaussian shaping of the channel transmit delay: the same window as the uniform draw, concentrated around a mean. Distribution tests are where vacuous checks hide most easily — a bound loose enough never to flake is usually loose enough to pass on a uniform draw — so the configured mean is deliberately OFF-CENTRE (1 in a `0..8` window, where uniform centres at 4) and the assertions are the ones a uniform draw fails: the sample mean sits within 0.5 of the configured mean, the mean's own bucket is the most frequent one, and every draw lands inside `[min, max]`. The control runs the same window with gauss off and requires the opposite — the flat mean must be OUTSIDE that tolerance, and the shaped peak more than twice the flat one — so "the numbers cluster" cannot pass on a CDF that was never built. Drawn from the config rather than the wire: `tc_chi_channel_delay` already proves the delay reaches the wire, and measuring a distribution through flits would re-test the plumbing instead of the shape. |
| `tc_chi_e_write_unique_zero` | COH | `WriteUniqueZero`: a snoopable full-line store of ZERO that puts NO data on the wire. The assertion that matters is the ABSENCE of a data phase — a `WriteUniqueFull` carrying a line of zeros would leave memory in exactly the same state and pass any readback check, so a test that only read the line back would pass just as loudly on the wrong opcode. Asserted: the REQ opcode that reached the wire, NO DAT flit from the requester, the line reading back as zero through a coherent read (not a peek into the home's memory model, so the same check means the same thing in both ports), and the other RN-F snoop-invalidated in both its cache and the directory. A second RN-F is given the line first on purpose: without a holder there is nothing to snoop and the `Unique` half would be untested while looking tested. |
| `tc_chi_e_write_evict_or_evict` | COH | `WriteEvictOrEvict`: the one CopyBack whose SHAPE the completer picks. The RN-F offers a clean line back and the home decides — `CompDBIDResp` means it wants the data (answered with `CopyBackWrData`, which is itself the implicit `CompAck`), `Comp` means it declines (answered with an explicit `CompAck`, degenerating into an `Evict`). BOTH legs run, driven deliberately through `cfg.hnf_write_evict_request_data`, because a test that drove one would leave the other as a branch nothing has ever taken — and they differ in the two things easiest to get wrong: whether data moves, and which acknowledgement closes the transaction. Asserted per leg: data leg sends `CopyBackWrData` and NO `CompAck` (an explicit one would be a response the home never expects); no-data leg sends no DAT at all and exactly one `CompAck`. `ExpCompAck` must be set on the REQ either way, and the line leaves the cache on both. |

## Multi-outstanding pipeline

Opt-in transaction overlap on the point-to-point RN-I↔SN-F path
(`cfg.multi_outstanding`), off by default so every other test stays strictly
serial. See [../docs/IMPLEMENTATION_PLAN.md](../docs/IMPLEMENTATION_PLAN.md).

| Test | Mode | Proves |
| --- | --- | --- |
| `tc_chi_d_multi_outstanding` | INT | six plain `ReadNoSnp` reads pipelined to a peak of 6 simultaneously in flight; each self-checks its address-derived `CompData` payload. |
| `tc_chi_d_multi_outstanding_write` | INT | six plain `WriteNoSnpFull` writes (`cfg.multi_outstanding_write`) pipelined to a peak of 6 in flight; each returns its `CompDBIDResp` completion. |
| `tc_chi_d_multi_outstanding_mixed` | INT | the unified loop (`cfg.multi_outstanding_mixed`) pipelines six `WriteNoSnpFull` writes then six `ReadNoSnp` reads to the same addresses on one driver; each read-back is checked against its captured write payload for end-to-end data integrity, with peak > 1 in flight. |
| `tc_chi_d_multi_outstanding_concurrent` | INT | seeds a read region, then forks six reads (of that region) and six writes (to a **disjoint** region) *concurrently* on one sequencer; proves reads and writes are in flight at the same instant (`observed_peak_mixed_inflight` > 1) while the seeded read-back stays intact under the opposite-direction traffic. |
| `tc_chi_d_multi_outstanding_partial` | INT | pipelines six `WriteNoSnpPtl` partial writes, each with its own data and byte-enable mask, to a peak of 6 in flight, then reads them back and checks the masked merge image (enabled bytes = written, disabled = zero background) survived the overlapped custom-BE data bursts. |
| `tc_chi_d_multi_outstanding_split` | INT | pipelines six writes against an SN-F using the split write policy (`cfg.split_write_rsp`): each is granted a `DBIDResp` then completed by a later deferred `Comp` rather than a combined `CompDBIDResp`. Confirms the pipeline overlaps these two-flit completions (peak 6), hands back `Comp`, and the data landed on read-back. |
| `tc_chi_d_multi_outstanding_compack` | INT | pipelines six `ExpCompAck` writes (peak 6): each carries an `NCBWrDataCompAck` burst and the RN-I must drive a `CompAck` back after completion. The SN-F times out if the ack is missing/mismatched, so passing proves the CompAck handshake overlapped correctly; read-back confirms the data landed. |
| `tc_chi_d_multi_outstanding_ordered` | INT | pipelines six ordered (`Order=Request_Order`) `ExpCompAck` writes (peak 6), whose ordering point is the CompAck the pipeline drives; confirms the REQ carried the Order value, the SN-F accepted every CompAck, and the data landed on read-back. |
| `tc_chi_d_multi_outstanding_ordered_read` | INT | seeds a region, then pipelines six ordered (`Order=Request_Order`) reads (peak 6). Each receives a `ReadReceipt` on RSP ahead of its `CompData`; the pipeline consumes the receipt and only retires the read once both arrive, then the read-back data is checked. |
| `tc_chi_d_multi_outstanding_atomic` | INT | seeds six granules, then pipelines six returning atomics (`AtomicLoad0` = arithmetic ADD, peak 6). Each issues write-like (`DBIDResp` grant + operand DAT) and completes read-like: the SN-F returns the pre-op value on `CompData` (matched by the DAT monitor) while the RMW writes seed+operand back. Confirms every atomic returned its own pre-op value and the read-back shows the overlapped read-modify-writes all landed. |
| `tc_chi_d_multi_outstanding_persist` | INT | pipelines six `CleanSharedPersist` CMOs (peak 6). Each carries no write data and takes no DBID grant — it issues its REQ and completes on a single `Comp` RSP. Confirms the pipeline overlaps these RSP-only, no-data transactions and hands each back with its `Comp`. |
| `tc_chi_e_multi_outstanding_persist_sep` | E | pipelines six CHI-E `CleanSharedPersistSep` CMOs (peak 6). Each completes with a two-part separated response — an intermediate `Persist` then a final `CompPersist`. The pipeline's RSP monitor consumes the intermediate `Persist` and retires each CMO on its `CompPersist`; confirms the two-flit, no-data completions overlap. |
| `tc_chi_d_multi_outstanding_retry` | INT | the retry path *inside* the pipeline: the SN-F (`force_retry_count=1`) bounces only the first of N retryable writes with `RetryAck` + `PCrdGrant`; it sits `retry_pending` awaiting its P-credit while writes 2..N complete around it, then re-issues concurrently. Asserts exactly one bounce, peak > 1 in flight, and every write (including the re-issued one) commits on read-back. |

## Credit / reset / link

| Test | Mode | Proves |
| --- | --- | --- |
| `tc_chi_d_retry` | INT | a retryable write (`AllowRetry=1`) against an SN-F armed with `cfg.force_retry_count` is bounced with `RetryAck` + `PCrdGrant`; the RN-I holds the request, consumes the credit grant, re-issues to a normal `CompDBIDResp`, and the data is confirmed committed on read-back. |
| `tc_chi_d_credit_starvation` | INT | hold SN-F DAT credit to stall a second read completion, then release it. |
| `tc_chi_d_lcrd_overgrant` | D | Negative control for `CHI_LCRD_OVERFLOW`, which could not fire before. §14.2.1 caps a receiver at **15** L-Credits per channel; the checker's bound was 64 — a number the specification does not contain — so a receiver granting 16–64 was over-granting and reported as fine, a false *negative*. The SN-F advertises 16 REQ credits and the sixteenth grant must be reported exactly once at each vantage, with the fifteen legal grants passing first and no further reports once traffic starts spending credits. The cfg validator still accepts 16 on purpose: a configuration that cannot express a broken peer cannot model one. |
| `tc_chi_d_lcrd_grant_cycle_negctl` | D | Wire negative control for `CHI_LCRD_USED_IN_GRANT_CYCLE`, which no configuration alone could reach. §14.2.1's Note forbids using an L-Credit in the cycle it is received, and the case needs the flit and the grant that authorises it sampled together with the send pool at zero — signals driven by opposite ends, so one half is set on each agent: the RN-I steps around its credit manager for exactly one REQ send, and the SN-F withholds its initial REQ advertisement and draws a single grant off the first inbound FLITPEND so it lands in the announced flit's own cycle. Both vantages must report exactly once, and `CHI_LCRD_UNDERFLOW` must stay silent throughout — the shadow applies the grant before the consume, so the count goes 0 → 1 → 0 and never dips, which is what hid this case and is why the two rules are not redundant. |
| `tc_chi_reset_idle_scope` | D | Both sides of a **closed list**. §14.1.3 / D §13.1.3 requires exactly `TX***LCRDV`, `TX***FLITV`, `TXLINKACTIVEREQ` and `RXLINKACTIVEACK` deasserted during reset, then closes the set: "All other signals can be any value." The four reset-idle rules also required FLITPEND and TXSACTIVE low, which §14.4 and §14.7 permit high by name — and they had **zero fails** across the whole regression, never once asked to fail. Phase 2 drives FLITPEND on every transmitted channel plus TXSACTIVE through reset and requires silence *and* that every rule re-evaluated; phase 3 drives `txrsplcrdv` through reset and requires a report at both vantages. The count is bounded (1..window-1) rather than exact, because an exact number would assert when the agent's reset handler happens to run. The SNP twin lives only on the coherent snoop binds and its control is still open. |
| `tc_chi_d_reset` | INT | reset while traffic is in flight, including held-credit recovery. |
| `tc_chi_d_link_reactivation` | INT | synthetic reset pulse, link drop, reactivation, and post-reset forward progress. |
| `tc_chi_e_signal_drivability` | INT | structured REQ/DAT/RSP field setters, separated return routing, and exact-E tagging fields reach the wire and are observed. |

## CHI-E focused (sidecar envs)

| Test | Mode | Proves |
| --- | --- | --- |
| `tc_chi_e_req_smoke` | E-RNI | exact-CHI-E RN-I loopback smoke for REQ-only E fields. |
| `tc_chi_e_dat_smoke` | E-RNI | exact-CHI-E RN-I loopback smoke for DAT tagging on write data. |
| `tc_chi_e_snf_dat_smoke` | E-SNF | exact-CHI-E manual SN-F completion smoke for responder-side DAT tagging. |
| `tc_chi_e_mte` | E-SNF-A | exact-CHI-E autonomous SN-F tag-storage round-trip for `TagOp`/`Tag`/`TU`. |
| `tc_chi_e_dbid_resp_ord` | E | exact-CHI-E ordered split write returns `DBIDRespOrd` before deferred `Comp` and trailing `CompAck`. |
| `tc_chi_e_rsp_field_legality` | E | SN-F RSP field applicability: a split DECERR write returns `DBIDRespOrd` with zero `RespErr` while the deferred `Comp` preserves `NDERR`, and `CleanSharedPersistSep` returns `Persist` with `TxnID = 0` while its `Comp` keeps the request TxnID. |
| `tc_chi_e_persist` | E | `CleanSharedPersist` retires on a single `Comp`; `CleanSharedPersistSep` has **two** legal completions and both are driven — `Comp` (the request reached the Point of Coherency) then `Persist` (it reached the Point of Persistence), and the two combined into a single `CompPersist` under `cfg.combined_persist_rsp`. A requester must accept both, so testing one would leave the other as a response the VIP claims to support and has never received. |
| `tc_chi_e_sep_read` | E | exact-CHI-E separated read: the Slave answers `ReadReceipt` + `DataSepResp`, never the Home-only `RespSepData`, and the data returns on `ReturnTxnID` (≠ request TxnID); exercises the scoreboard Checker-A return-index so the separated completion is matched rather than false-orphaned. |
| `tc_chi_e_sep_read_negctl` | E | negative control for `CHI_SB_ORIGINATOR_LEGAL` in two phases: a Slave emitting the Home-only `RespSepData` (Table B-3 gives it one `From` row, `ICN(HN-F, HN-I)`), then the Home stand-in switched off so the VIP's own documented departure — `ReadNoSnpSep` from an RN-I — is reported rather than exempted. |

## HN-I proxy suite

These route through the `vip_chi_hni_agent` proxy (see
[sv/UVM_TB.md](sv/UVM_TB.md) §1–3 and [../README.md](../README.md)). Because the
RN-I monitors watch the RN↔HN links
and the SN-F monitors watch the HN↔SN links, these tests verify both that the RN
saw correct completions **and** that the SN actually received the forwarded
request — i.e. the proxy relayed rather than short-circuited.

| Test | Mode | Proves |
| --- | --- | --- |
| `tc_chi_d_hni_passthrough` | HNI | 1×1 relay: write + readback through RN-I → HN-I → SN-F; SN-F observed the forwarded requests; readback payload matches. |
| `tc_chi_d_hni_fanin` | HNI-FANIN | two RNs (distinct node ids) fan into one SN; each RN reads back its own payload → node-id completion return works. |
| `tc_chi_d_hni_xbar` | HNI-XBAR | request **address** decodes RN0→SN0 and RN1→SN1 (stride); each SN-F saw only its own address; each RN read back its own data. |
| `tc_chi_d_hni_sam` | HNI-XBAR | same split driven by a configurable **SAM range table** using addresses that share the stride bit (so only the ranges can split them). |
| `tc_chi_d_hni_qos` | HNI-XBAR | two reads to one SN with different QoS presented together (arbitration collection window); the SN-F observes the high-QoS request first. |
| `tc_chi_d_hni_decerr` | HNI | SN-F DECERR/NDERR error responses relayed intact through the proxy for write and read. |
| `tc_chi_d_hni_atomic` | HNI | an `AtomicStore` relayed end-to-end (operand DAT forwarded to the SN, completion returned; exercises the write-settle path). |
| `tc_chi_d_hni_persist` | HNI | a completion-only `CleanSharedPersist` relayed; exercises the forwarder's RSP-only settle path (no DAT in either direction). |
| `tc_chi_d_hni_split_write_rsp` | HNI | proxied writes into an SN-F using split `DBIDResp` + deferred `Comp`; confirms the HN-I holds the RN REQ credit until both DAT and `Comp` have crossed. |
| `tc_chi_e_hni_passthrough` | E-PROXY | the same 1×1 relay at CHI-E wide (64 B = one DAT beat): write + readback through the CHI-E HN-I proxy, SN target 0 observed both forwarded requests. |
| `tc_chi_e_hni_port1` | E-PROXY | **SV only** — the CHI-E proxy's second RN port and second SN target, which nothing else drives. Requires the seven per-opcode REQ field rules to have been *evaluated* at all four port-1 vantages, so a bind that never runs cannot pass as a clean link. The Python CHI-E proxy is 1×1, so those binds do not exist there. |
| `tc_chi_snp_flit_layout` | LAYOUT | the SNP flit's field order against D Table 12-8 / E Table 13-8, asserted twice: the declared order, and where the bits actually land when one field at a time is set to all ones. Includes `MPAM_EN_P`/`mpam_en` = 1, the only setting under which the 11-bit MPAM field exists. Topology-free — the flit types are what is under test. |
| `tc_chi_coh_e_snp_field_negctl` | SNP-NEGCTL | one `SnpCleanInvalid` with three corrupted fields — it is in all three of the specification's sets at once — provoking `CHI_SNP_FWD_FIELDS_ZERO`, `CHI_SNP_RET_TO_SRC_LEGAL` and `CHI_SNP_DO_NOT_GO_TO_SD_LEGAL` exactly once each at **both** vantages, and requiring that no other check fired. CHI-E only: Issue D has no `DoNotGoToSD` must-be-one list, so one of the three has nothing to provoke there. |

## Coherent subsystem (RN-F / HN-F / SNP)

The coherent env (`chi_coherent_tb_env`) drives two RN-F requesters against a
single multi-port HN-F home node, which terminates to its own `vip_mem` and owns a
per-line directory. Coherent reads (`ReadShared`/`ReadClean`/`ReadUnique`) that hit
another holder make the HN-F originate snoops on the SNP channel; each RN-F answers
autonomously from its own cache-state model. Every test self-checks the RN-F cache
state and the HN-F directory, and the always-on **Checker D** (`vip_chi_coherency_checker`)
watches a self-derived ownership shadow for its single-writer invariant (never two
Unique owners of a line). Coverage spans clean reads + snoops, dirty snoop
forwarding (`SnpRespData` + PassDirty), and eviction (`WriteBackFull` /
`Evict`).

| Test | Mode | Proves |
| --- | --- | --- |
| `tc_chi_coh_d_read_no_snoop` | COH | a single RN-F `ReadShared` with an empty directory: the HN-F returns `CompData(SC)` from its memory, the RN-F caches `SC`, the directory records `SC`, and **zero** snoops are issued. |
| `tc_chi_coh_d_read_then_unique` | COH | RN-F0 `ReadShared`→`SC`, then RN-F1 `ReadUnique` to the same line makes the HN-F snoop RN-F0 (`SnpUnique`); RN-F0 drops to `I`, RN-F1 is granted `UC`, directory tracks the transfer. |
| `tc_chi_coh_d_shared_read` | COH | RN-F0 `ReadUnique`→`UC`, then RN-F1 `ReadShared` makes the HN-F snoop RN-F0 (`SnpShared`) to downgrade it; RN-F0 becomes `SC`, RN-F1 is granted `SC`. |
| `tc_chi_coh_d_negctl` | COH | **negative control for Checker D**: with `cfg.hnf_suppress_snoops` the HN-F grants Unique without invalidating other holders, so two RN-F `ReadUnique`s to one line leave two Unique owners. The test fails unless Checker D flags the violation (`multi_owner` > 0); the induced error is caught and demoted so it does not count against the regression. |
| `tc_chi_coh_d_line_hazard` | COH | the same-cache-line hazard rule, both halves. Two coherent reads to one line issued strictly one at a time must NOT be flagged (the rule must key on overlap, not on address reuse); two overlapping same-line observations must be flagged exactly once; and a re-issue on the same TxnID must not be, since obeying the retry protocol is not a hazard. The overlapping pair is published into the checker's REQ port rather than driven, because this VIP's RN-F issue path is serial and its multi-outstanding pipeline refuses coherent opcodes — there is no requester configuration that produces the fault. |
| `tc_chi_coh_d_dirty_forward` | COH | dirty snoop forwarding: RN-F0 acquires a line Unique and dirties it (modelled local store), then RN-F1 `ReadShared` forces a downgrading snoop; RN-F0 answers with `SnpRespData` (PassDirty), the home merges the modified beats into memory, and RN-F1's `CompData` carries the dirtied data (asserted == RN-F0's original beats XOR the store pattern, not stale memory). |
| `tc_chi_coh_d_req_retain` | COH | requester final state (IHI 0050 E Table 4-14 / D Table 4-12): RN-F1 `MakeUnique` a line (-> UD, observable), dirties it locally, then issues `ReadClean` on the SAME line and is granted the WEAKER `CompData_SC`. Asserts the line stays UD in the RN-F cache, in the checker's shadow and in the home's snoop filter (footnote b), that the locally-modified beats survive the fetch (footnote c) by having RN-F0 read them back through a forwarding snoop, and that the checker recorded the completion as held-state-decided. |
| `tc_chi_coh_d_req_final_state_negctl` | COH | negative control for the above: `rnf_req_final_state_verbatim` puts RN-F1 back on the granted `Resp` verbatim, so its dirty copy is lost and the following snoop is answered with no data. Catalogue rule D7 must flag it (`snp_dirty_lost` rises); the induced error is caught and demoted. |
| `tc_chi_coh_d_read_clean_snoop` | COH | request->snoop correspondence (IHI 0050 E Table 4-5 / D Table 4-3): a `ReadClean` must be snooped with `SnpClean` on the ordinary path and `SnpCleanFwd` on the Direct Cache Transfer path. Drives both, asserting the observed snoop opcode each time, and that catalogue rule D8 judged at least one snoop against its request with no mismatch. Before Table 4-5 was modeled both were `SnpShared`/`SnpSharedFwd`. |
| `tc_chi_coh_{d,e}_do_not_go_to_sd_negctl` | D, E | Negative control for the **DoNotGoToSD obedience** rule. The SNP channel already judges whether the bit was *set* where §13.10.35 requires it; this judges whether the snoopee *obeyed* it, which is the half a third-party DUT can get wrong. `cfg.rnf_snp_resp_sd_negctl` makes the snoopee report SD — needed because the responder's state map cannot produce SD at all, so the rule had nothing in the regression that could exercise it. **Both cuts are asserted and they differ**: under E the bit is must-be-one so the rule must report; under D §12.9.32 lets it take any value, so the cleared bit is conformant and the rule must stay silent — which is what shows the rule reads the bit rather than the state. Neither neighbouring rule catches it: D5 bounds the reported state by opcode and SD is not Unique; D6 bounds it by what the snoopee held, and an SD holder answering SD passes that too. The provocation is **declared** in both ports — the pyUVM half through `chi_coherency_negctl_catcher`, which demotes the induced reports and counts them, and the count is held against the movement in the rule tallies so a report escaping the catcher or a tally moving without a report both fail. D6 is named explicitly in that accounting because it is easy to miss: it reports like any violation but sits between two purely observational counters. |
| `tc_chi_coh_{d,e}_snp_resp_data_negctl` | D, E | Negative control for the **snoop response-form** rule. IHI 0050 §4.3 defines `SnpMakeInvalid` by the property that it returns no data and the snoopee discards its dirty copy, and Tables 4-9/4-11 list no `SnpRespData` form among its permitted responses — so what is prohibited is the *pairing*, not the encoding, and a checker written on encodings alone cannot see it. A conforming snoopee never emits the pairing, which leaves the rule's zero in every other run unfalsifiable: `cfg.rnf_snp_resp_data_negctl` puts a dirty holder back on the data-bearing path for a no-data snoop, corrupting the *decision* rather than the flit, so the response is one a real snoopee could emit. RN-F0 takes the line with `MakeUnique` (Unique-Dirty, with beats) and RN-F1 issues `MakeInvalid`, which makes the home send `SnpMakeInvalid`. The neighbouring rules are asserted **silent**: the reported state is Invalid, which D5 permits, and D7 excludes this opcode because discarding is what it asks for. The pairing is also one of `cg_snp_resp_legality`'s illegal bins, which no catcher can demote and which ends the run on the hit, so the test declares `expect_illegal_snp_resp` to suppress that sample alone — every rule still fires. The Checker-D report is **declared** in both ports, with the demoted count held against the response-form tally: since the state rule is asserted silent, the form rule is the only source the catcher can have had. |
| `tc_chi_e_write_zero_form_negctl` | E | Negative control for the **zero-write completion form**. `WriteNoSnpZero` is answered by `DBIDResp` and a `Comp`, or by a combined `CompDBIDResp`; a bare `Comp` is neither. The request carries no write data, so the granted buffer is never used and the DBID looks pointless — which is exactly why a completer leaves it out and why a requester that had never been told otherwise accepts it. The refusal is unreachable from conformant traffic, so its silence elsewhere says nothing: `cfg.snf_write_zero_bare_comp_negctl` provokes it. Asserted as **exactly one** refusal and on the *first* response — a requester that waited for a grant before objecting would hang rather than refuse. The refusal is a `uvm_fatal` in SV, demoted and counted through `chi_sb_rule_negctl_catcher`; the pyUVM twin reaches the same place through `reject()` and an `expect_rejection` scope. |
| `tc_chi_e_persist_sep_form_negctl` | E | Negative control for the **separated-persist completion form**. A requester must accept `Comp` then `Persist`, or `CompPersist` alone, and only those two. `Persist` then `CompPersist` is neither: no bare `Comp` ever arrives and persistence is signalled twice. It is also the shape this VIP's own completer used to produce, which is why its requester used to demand it — the two agreed with each other and were wrong together, the failure mode a control on *one* side cannot find. `cfg.snf_persist_before_comp_negctl` reproduces it, TxnID included, so `CHI_RSP_FIELD_ZERO` judges the same flit independently (Table A-4 requires a standalone `Persist` to carry TxnID = 0) — asserting both is what separates a completer put back on the old shape from one that merely sent an unexpected opcode. **Two** requests, because the requester's first wait is TxnID-matched and two consecutive requests cannot both draw zero. `CHI_TXSACTIVE_COVERS_OUTSTANDING` is declared **and counted** as inherent collateral: a refused completion is never retired, so the transaction stays outstanding while the activity window closes over it. |
| `tc_chi_lasm_divergent_states` | D | The two **link state machines in different states in the same cycle**, and the transition rule required to stay *silent* through it. §14.6.1 defines a transmit machine and a receive machine per interface and §14.6.2 says Figure 14-5 is drawn so "the independent nature of the Tx and Rx state machines can be seen" — so a component whose transmit link is RUN while its receive link is still ACTIVATE is conformant. This is the case the checker could not represent while the four sideband signals were OR-collapsed: `TxRun/RxAct` and `TxAct/RxRun` both computed `req=1, ack=1` and aliased onto RUN, and a peer advancing both machines in one cycle could move the collapsed state two places and be **reported for a legal step** — a false failure against conformant hardware. A control on *silence* needs its provocation asserted or it proves nothing: `cfg.lasm_stall_activation_cycles` holds the completer's own request and acknowledge back, and `lasm_divergent_cycles` counts the cycles the two really did differ (12 at each end, both ports). Zero reports from the step rule *and* from `CHI_LASM_ACTIVATE_OBSERVED`, with the step rule's pass count asserted non-zero so its zero is not the zero of a rule that never ran. |
| `tc_chi_e_dwt_dbid_return_nid{,_negctl}` | E | Where a **Direct Write Transfer's DBIDResp** is addressed. IHI 0050 E Table 2-8 routes the grant to `ReturnNID` when `DoDWT = 1`, and §2.5 sends the TxnID with it — `ReturnTxnID`, not the request's own. Both fields are moved off the requester, and to *different* values, so a completer that got one right and the other wrong cannot pass. The limb was **unreachable** rather than merely unchecked: `DoDWT` is REQ bit 17, shared with `SnpAttr`, and until the field identity was fixed every sequence pinned it to zero. The negative control (`cfg.snf_dwt_dbid_target_srcid_negctl`) keeps the grant at `SrcID`/`TxnID` and requires **two independent observers** to object: `CHI_SB_RSP_TGTID_CORRECT` reading the address off the wire, and the requester refusing a grant it is not listening for. Writing it found two further defects — the item constraint zeroed `ReturnTxnID` on every opcode but `ReadNoSnpSep`, making a conformant DWT write unrandomizable; and `CHI_REQ_ATTR_COMBINATION_LEGAL` read bit 17 as `SnpAttr` unconditionally, faulting every DWT request against Table 2-12. |
| `tc_chi_e_combined_write_{comp_persist,cmo_first,obligation_negctl}` | E | The combined Write + CMO **obligation set**. The requester used to demand one fixed completion sequence — write completion, then exactly `CompCMO`, then exactly `Persist` — and `uvm_fatal`ed on anything else. Issue E permits two shapes it rejected: `CompPersist` in place of `CompCMO` + `Persist` (§2.8, permitted when both target the same node), and `CompCMO` **before** the write's `Comp` (§2.8 orders CompCMO only against the request, never against the write completion). Neither was a mis-score — both were a stopped simulation on legal CHI. Completion is now *both obligations met*, which is how the scoreboard already modelled it. Both positive tests read back the requester's `combined_completion_log` and assert **which** response retired each obligation, because a completer that ignored the knob and sent the default would complete too and pass a weaker test. The negative control sends a **second `CompCMO`** — deliberately a response this transaction is entitled to, at a moment when it satisfies nothing outstanding, so an implementation keyed on "legal opcode" rather than "obligation still owed" fails it. Writing it found that the scoreboard's `CompPersist` handler ticked Comp+Persist on every transaction, when §2.8 makes it **CompCMO**+Persist on a combined write. |
| `tc_chi_coh_e_snp_query` | E | **`SnpQuery`** (SNP `0x10`), the snoop that asks and changes nothing. IHI 0050 E §4.5 gives the home a snoop it may send "without any corresponding request from a Requester", requires the response to carry "the precise state of the cache line at the targeted Snoopee", forbids the snoopee to return data, and forbids the snoop to change the state at all; Table 4-26 repeats the last of those on every one of its seven rows. `cfg.hnf_snp_query_enable` puts the home on it: instead of reading its own directory it asks every port the directory says holds the line, and compares. The stimulus is built so the answer is one the ENCODING cannot give. RN-F0 takes the line `ReadUnique` (the home records UC) and then dirties it locally -- a silent transition the home never sees -- so the port holds UD while the filter holds UC. Table 4-9 gives UC and UD **one** Resp encoding, because "Pass Dirty must only be asserted for a Snoop response with data" and `Resp[2]` is that bit, so the correct answer from a UD holder is `0b010`, the home's expectation from UC is the same `0b010`, and the two agree: a query cannot detect a silent clean-to-dirty transition and must not claim to. A responder reporting its raw `UD_PD` would put a Pass Dirty bit on a data-less `SnpResp` and be flagged for a mismatch it did not commit. Asserts the query was sent and answered, that catalogue rule D10 judged it and found nothing, that the reconciliation agreed with the filter, and that the dirty copy survived to reach RN-F1 through the `SnpShared` that follows. |
| `tc_chi_coh_e_snp_query_negctl` | E | Negative control for catalogue rule **D10**, the state-preserving snoop. `cfg.rnf_snp_query_mutates_negctl` makes the snoopee invalidate the line under the query and answer `SnpResp_I` -- a **self-consistent** response, reporting exactly what the responder did, which is what makes it a control rather than a broken flit. Every neighbouring rule passes it: D5 bounds the answer by what the opcode asked for and a query asks for nothing; D6 bounds it by what the snoopee held and `I` claims less, not more; D7 wants the dirty copy accounted for and excludes the snoops that return no data, which is the set `SnpQuery` is in. A line can therefore be lost to a query with every other rule agreeing -- the gap D10 exists for. Asserts not only that a violation was reported but that **D10** reported it and that **nothing else did**: the catcher's demoted count is held against D10's own tally, so a run where some other rule also fired -- one in which D10 could be deleted and the test still pass -- fails. |
| `tc_chi_coh_d_snoop_match_negctl` | COH | negative control for the above: `hnf_snoop_shared_for_read_clean` puts the home back on the single `is_unique` bit. The same knob then yields `SnpShared` (which the bullet under Table 4-5 PERMITS for `ReadClean`, so D8 must stay silent) and `SnpSharedFwd` (which no bullet reaches, so D8 must fire). Asserts both halves; the induced error is caught and demoted. |
| `tc_chi_coh_{d,e}_comp_resp_negctl` | D, E | Negative control for **`CHI_RSP_COMP_RESP_LEGAL`**, the Resp encodings a `Comp` may carry (IHI 0050 E Table 4-7 / D Table 4-5). The injection is the defect this VIP actually shipped: `cfg.hnf_comp_resp_negctl` puts the home back on `Comp_UD_PD` for a `MakeUnique`, where both tables give `Comp_UC`. **The same injection is legal under one issue and not the other, and both answers are asserted** — E lists `Comp_UD_PD` so the rule must stay *silent* and record a **pass**; D permits exactly `Comp_I`/`Comp_UC`/`Comp_SC` and gives `0b110` no meaning on a `Comp` at all, so the rule must *fire*, at both vantages. A control reporting on both cuts would pass equally well against a rule that ignored the issue and checked E's larger set everywhere — which is what let the original CHI-D defect through. Proved two-sided by mutation: allowing `UD_PD` unconditionally fails only the D cut, forbidding it unconditionally fails only the E cut. The coherency checker objects on **both** cuts (Table 4-19 gives `MakeUnique` one completion response in either issue), asserted separately and declared through the catcher — the encoding rule judges the flit, the row rule judges it against the request that caused it. |
| `tc_chi_coh_{d,e}_fwd_target_negctl` | COH | negative control for the **FwdNID/FwdTxnID value** rule. The SNP channel bind judges only the half a link-layer checker can see — both fields must be zero on a snoop that is not one of the six forwarding forms — and passes *any* value on the six that are, because the flit alone does not say which request the home meant to name. IHI 0050 E §2.5 also states the positive half: FwdNID must be the Node ID of the original Requester and FwdTxnID the TxnID of the original Request, so it is judged in the coherency checker, on the same request-to-snoop correlation catalogue rule D8 already resolves. `cfg.hnf_snp_fwd_target_negctl` **adds one** to each field rather than substituting a constant: every requester on this bench drives SrcID zero, so a constant zero would corrupt nothing and a constant one would stop corrupting the day a test gives them real Node IDs. Both phases are asserted, and the conformant one is the load-bearing assertion — it requires the rule to have RECORDED A PASS, since a checker that rejected every forwarding snoop would satisfy a control written on failures alone and false-fail the three DCT tests. Exactly **4** reports over **2** corrupted forwards: the injection latch is per port and by the second phase both directions forward, and FwdNID and FwdTxnID are judged separately so the message says which half broke. Each arm proved load-bearing by mutation. |
| `tc_chi_coh_d_comp_ack_read` | COH | the read half of `CompAck` (IHI 0050 E Table 2-9 / D Table 2-8, section 2.8.3): an RN-F `ReadShared` must carry `ExpCompAck` and answer its `CompData` with a `CompAck`. Asserts the acknowledgement window opened and closed, then has the other RN-F read the same line and asserts the resulting snoop fell OUTSIDE that window -- section 2.8.3 rule 2, judged by catalogue rule D9. Before Table 2-9 was modeled the item constraint forced `ExpCompAck` to zero on every read. |
| `tc_chi_coh_d_comp_ack_window_negctl` | COH | negative control for D9: `hnf_snoop_before_comp_ack` makes the home send one `SnpOnce` into the window between `CompData` and `CompAck`, with the requester's RSP delayed so the window is unambiguously open. `SnpOnce` perturbs neither state nor data, so only the timing rule can fire. Asserts the in-window snoop count moved; the induced error is caught and demoted. |
| `tc_chi_coh_d_expcompack_negctl` | COH | negative control for `CHI_EXPCOMPACK_REQUIRED_BUT_ZERO`: `rn_drop_required_exp_comp_ack` clears the bit on a `ReadShared`, which Table 2-9 marks required. The item field is cleared, not just the flit, so the requester also sends no `CompAck` -- asserted, so exactly one rule fires. Also asserts no D9 window opened, i.e. the rule arms off the wire bit. |
| `tc_chi_coh_d_writeback_evict` | COH | both eviction paths: RN-F0 `WriteBackFull` (DBID grant → `CopyBackWrData` → memory commit) and RN-F1 `Evict` (RSP-only `Comp`). Both directory ports and both RN-F cache states return to Invalid. |
| `tc_chi_coh_d_read_after_writeback` | COH | writeback data integrity: RN-F0 writes a fresh payload back, then RN-F1 `ReadShared` returns exactly the written-back data (and it differs from the original image — a no-op writeback would fail). |
| `tc_chi_coh_d_write_unique_ptl` | COH | `WriteUniquePtl` data integrity: RN-F1 writes 16 byte-enabled bytes at offset 16, the HN-F invalidates RN-F0 and commits at the request address, and a full-line readback proves only the enabled lanes changed. |
| `tc_chi_coh_d_txsactive_window` | COH | `TXSACTIVE` at the HOME, and the half a single-requester run cannot reach: a home serves one request at a time out of a queue, so the interval a captured request spends WAITING is part of the window IHI 0050 E section 14.7.2 / D section 13.7.2 asks for. RN-F1 reads a line RN-F0 owns so the home must snoop first, and RN-F0's own read of a different line is launched into the middle of that round trip. On the home's port 0 the sideband must be up before its first response flit, stay up across the whole wait, cover at least one flit-free cycle, and drop once the traffic drains -- failing which the window opens at dispatch, is pulsed per flit, or is driven from link-up respectively. |
| `tc_chi_coh_d_txsactive_negctl` | COH | **negative control for `CHI_TXSACTIVE_COVERS_OUTSTANDING` at the home**: `cfg.hnf_txsactive_early_drop_negctl` retires the home's RN-facing window the moment it starts serving a read instead of when the transaction completes, and the rule must report the cycles the sideband spends low under an in-flight transaction. The read is aimed at a line the other requester owns so the home must snoop first, making the window tens of cycles wide. Dropped at the START of service, not before the CompAck: the rule's shadow retires a read when its CompData is on the wire, so a drop after that is legal over-assertion the rule is right to ignore. Asserts `CHI_TXSACTIVE_DEASSERT_BOUNDED` stays silent, which keeps the control pointed at one rule. |
| `tc_chi_coh_d_snp_txsactive_negctl` | COH | **negative control for the RECEIVING limb of `CHI_TXSACTIVE_COVERS_OUTSTANDING`, at the snoopee**: §14.7.2 gives the snoopee a sentence of its own — "An RN-F or RN-D component must also assert TXSACTIVE while a Snoop transaction is in progress" — and `cfg.rnf_txsactive_snoop_drop_negctl` makes RN-F0 answer a snoop without opening a window at all. RN-F0 takes the line Unique and its own read is allowed to RETIRE before RN-F1 reads, so the snoop lands on a node with nothing outstanding of its own and the request limb cannot hold the sideband up. Reads the count at the snoopee's own bind, requires `CHI_TXSACTIVE_DEASSERT_BOUNDED` to stay silent, and requires the home's own sideband to be untouched. The arm carries a MEASURED two-cycle assertion allowance: at one cycle every legal coherent run false-fails. |
| `tc_chi_coh_d_snp_link_gate_negctl` | COH | **wire negative control for `CHI_SNP_FLITV_REQUIRES_LINK`**, previously reachable only in a unit test. §14.6.1 gives an interface two link state machines and a snoop belongs to the home's **transmit** one, which reaches RUN only when the snoopee acknowledges — and at a coherent endpoint the two moved together. `cfg.lasm_ack_delay_cycles` separates them by holding the snoopee's acknowledge down, which is a *conformant* peer (Table 14-2's dwell in ACTIVATE); delaying the home's request instead cannot work, because §14.6.3 forbids an acknowledge to lead its own request. What the delay exposes is a defect at the home, fixed here: `rn_activate` waits for the acknowledge before opening the transmit gate, and `cfg.hnf_send_before_tx_link_negctl` stands that gate down. `CHI_DAT_FLITV_REQUIRES_LINK` reports alongside on both ports and is asserted rather than waived — one gate covers the whole transmit link. **Phase 2** is the other rule of the pair: §14.6.3 makes `CHI_SNP_LCRDV_REQUIRES_LINK` unreachable during a first bring-up, so `cfg.rnf_snp_credit_before_link_negctl` puts part of the initial SNP advertisement ahead of the link — and that alone still reports nothing, because the rule is gated on the link having *ever* been active. A **reset** satisfies both: it returns an already-active link to STOP. Exactly two reports, at the snoopee and nowhere else, with the pre-reset silence asserted too. Also `tc_chi_coh_e_snp_link_gate_negctl`. |
| `tc_chi_coh_d_partial_dirty_merge` | COH | **the MERGE half of Table 4-14 footnote c**: "data received from memory must be dropped if the cache state is UD or SD, or **merged** if the cache state is UDP". UDP is a state the wire cannot express — Table 4-6 encodes it as `UD_PD`, the same as UD — so the requester now keeps a per-beat dirty **byte mask** beside the state, and that mask is the whole difference. The line is acquired with `MakeUnique` (Unique-Dirty, no data, so no byte of it is real memory), then partially stored into, then read. All three outcomes are wrong but one: taking the fetch loses the store, dropping it keeps bytes the cache was never given, merging is right. Both failure modes get their own assertion, and the drop arm is not hypothetical — it is exactly what the VIP did before the mask existed. The test refuses to pass on a zero memory image, where merge and drop agree. Also `tc_chi_coh_e_partial_dirty_merge`. |
| `tc_chi_coh_d_evict_allocate_negctl` | COH | **negative control for `CHI_REQ_ALLOCATE_LEGAL`, and the proof it is not redundant**: §2.9.3 puts Evict on the Allocate field's inapplicable-and-must-be-zero list and Table A-3 gives it a literal zero, but Table 2-12's Snoopable rows leave the bit free — so an Evict carrying it is a legal tuple with an inapplicable field set, and nothing else in the checker can see it. Issues the Evict with MemAttr = {Allocate 1, Cacheable 1, Device 0, EWA 1}, which is row-legal against the SnpAttr = 1 the opcode requires, and requires the rule to report at BOTH vantages while `CHI_REQ_ATTR_COMBINATION_LEGAL` and `CHI_REQ_SNP_ATTR_LEGAL` stay silent. The Evict must still complete and the directory must still clear. |
| `tc_chi_coh_{d,e}_lasm_deactivate` | COH | **graceful deactivation on the coherent link, which reset alone can never reach and which three separate defects made impossible.** The requester's half of the tear-down was already written, so none of them was visible from that end: the home's REQ ingress had no `ReqLCrdReturn` arm and fatalled with *"unsupported REQ opcode 0x0"*; the home had no per-port `link_deactivating`, so it kept granting into a link that was coming down and dropped its acknowledge two cycles after the request fell whatever it still held; and the RN-F's `snp_credit_loop` had no stand-down. The **fourth channel** is what makes this link different from the one that already worked: SNP runs home to requester, so the home holds the send credits and the requester grants them, and `CHI_LCRD_QUIESCENT_IN_STOP` cannot see any of it -- the SNP rules live in their own bind so a non-coherent link elaborates none of them. Hence `CHI_SNP_LCRD_QUIESCENT_IN_STOP`, and hence the reading taken **before** the tear-down: the home must already hold snoop credits and the requester must already count them as granted, or every SNP assertion afterwards is satisfied by a link that never put a snoop credit anywhere. Port 1 stays up throughout, because a home that could only tear down by taking every port with it would pass a single-port test. Phase 3 snoops again and counts the snoop off the monitored channel rather than the credit off the driver -- a re-granted credit that never carried a snoop would not prove the channel came back. |
| `tc_chi_coh_d_hnf_sn_txsactive` | COH | **the home's SN-facing `TXSACTIVE` is a window around its own downstream requests, not a level that follows the link.** §14.7.2 requires the interconnect interface to an SN to assert it before or with the initiating Request flit and hold it until after the final completing flit. The home drove it from `sn_link_up`, so it was high from bring-up to tear-down whatever it had downstream -- legal by the letter, since over-assertion always is, and carrying nothing. Both halves of the sideband were unwatched at that vantage: the bind stood `CHI_TXSACTIVE_DEASSERT_BOUNDED` down for exactly that drive, and `CHI_TXSACTIVE_COVERS_OUTSTANDING` evaluated **zero times** there. A per-cycle trace of the home's SN port, taken with the link already up: `TXSACTIVE` must be **low before** the first downstream flit -- the claim a link-driven sideband cannot satisfy at all -- **high on every cycle a flit moves**, high on at least one cycle with no flit so it is a held window rather than a per-flit pulse, and **low again** once the traffic drains. The downstream `ReadNoSnp` is counted separately, because a run where the home answered from its own memory would satisfy every trace assertion but the first, and that one only by accident. |
| `tc_chi_coh_e_combined_write_cmo` | COH | **the coherent half of the combined Write + CMO family**: one request carrying a write to the Home and a cache-maintenance operation on the same address, applied in that order. The WriteNoSnp forms reach a memory node and were built first; these reach the HN-F, so the write half runs on the coherent completer path -- `service_writeback` for the CopyBacks, `service_write_unique` for the rest -- and the CMO acts on the state that write leaves behind. Six forms across the three write classes and both persistence choices. `CompCMO` is what says the CMO half happened: the write completes exactly as an ordinary write does, so a completer that ignored the CMO entirely would produce an indistinguishable run. Read off the requester's accepted-completion log rather than the wire, because a response left in the stream would surface as a wrong-opcode error on the NEXT transaction instead of a missing entry here. A persistent form must draw a `Persist` and a non-persistent one must not, both directions asserted so the check cannot pass by always expecting one. Cache state is asserted per write class -- a CopyBack ends Invalid, a WriteClean keeps a clean copy, a WriteUnique is non-allocating -- and the `CleanInvalid` phase seeds a second holder first, because it is the one CMO of the three with work left to do and without a holder the phase would pass against a home that skipped the CMO. CHI-E only: every form sits in the `Opcode[6] = 1` half of Table 13-14 and does not fit CHI-D's 6-bit REQ opcode field. The persistent phases also assert the **`Persist`'s PGroupID**, read off the wire and pinned to a value with bits set in both halves of 13.10.8's `PGroupID[7:0] = {GroupIDExt[2:0], LPID[4:0]}` -- so a completer that built the field out of one half alone fails, and so does the state this test was written against. This is the scenario that stands up `chi_coherent_e_tb_env`: the SystemVerilog coherent link used to run the **base** agents at CHI-E width, so `GroupIDExt` never reached the wire, the home had nothing to read back, and both vantages agreed on group **zero** -- consistent, and consistently wrong, which is why no parity check, counter or scoreboard rule could see it. Zero is precisely what a silent link reports, which is why the pinned group is not zero in either half. |

## Instrumentation & checker guards

Anti-vacuity guards for the always-on measurement / checking components: each
induces the condition its component must observe and fails if the component stays
silent, so a disabled or disconnected checker cannot pass unnoticed.

| Test | Mode | Proves |
| --- | --- | --- |
| `tc_chi_d_perf_smoke` | INT | guard for `vip_chi_perf_counters` (latency / throughput / retry / back-pressure off a reset-gated cycle counter): drives reads and writes then fails unless the read/write completion counts, the latency accumulator, and the cycle counter are all non-zero. |
| `tc_chi_d_scoreboard_negctl` | INT | guard for the always-on `vip_chi_scoreboard`: after one clean write it raw-injects an orphan `Comp` RSP (bogus TxnID); a report catcher demotes the induced "Orphan RSP" error and the test fails unless the checker actually fired. |
| `tc_chi_dataid_duplicate` | INT | guard for the monitor's DataID placement checks: `snf_duplicate_dat_beat` makes the SN-F send the last beat of a read burst carrying `DataID` 0 again, so one position arrives twice and one never; a report catcher demotes both induced errors and the test fails unless BOTH the duplicate and the missing-beat check fired. Arrival-order reassembly sees neither fault -- the beat count still adds up. |
| `tc_chi_pcrd_return` | INT | the other half of the retry handshake: `tc_chi_pcrd_leak` proves the requester NOTICES a granted-and-unused P-credit, this proves it can GIVE ONE BACK. Same stimulus as the leak test — pipelined RN-I, a raw `PCrdGrant` that bounced nothing — with `cfg.return_unused_pcrd` on, so the verdict flips from "leak reported" to "credit returned". Asserts the `PCrdReturn` as an OBSERVED REQ flit, not just a drained internal bank: draining without emitting would satisfy every counter and return nothing to the completer. Its identifier fields are checked against the rules the specification fixes for this transaction — `PCrdType` matching the grant, `TxnID` zero, `TgtID` the granter. |
| `tc_chi_rsp_field_zero_negctl` | NEG | negative control for `CHI_RSP_FIELD_ZERO`, the Appendix A Table A-4 rule that a field marked `0` or `0 a` must be driven zero. The rule was written after the two defects it would have caught were already fixed, so it passes everywhere and a rule that only ever passes cannot be told from one that cannot fail. Injects four raw `PCrdGrant` flits, one per zero-marked field (`TxnID`, `RespErr`, `Resp`, and the shared `DBID` span), and asserts the count was zero beforehand and is **exactly** four afterwards at BOTH ends of the link — fewer means a field is uncovered, more means the rule is also firing on compliant traffic, and checking one end only would leave the other vantage of a two-sided rule silently untested. |
| `tc_chi_pcrd_leak` | INT | guard for the RN-I's end-of-test P-credit accounting: with the pipeline enabled (the path that banks credits) a bare `PCrdGrant` that bounces nothing is injected from the SN-F; a report catcher demotes the induced leak error and the test fails unless the driver's `check_phase` actually reported it. A leaked P-credit changes nothing observable otherwise — the traffic completes and the run goes green. |
